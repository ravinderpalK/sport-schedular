<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/tailwind.css">
  <title>
  </title>
</head>

<body>
  <div class="grid grid-cols-6">
    <div class="col-start-3 col-span-2">
      <div class="text-lg pt-2">
        <a class="underline" href="/">Home</a>
        <span> > </span>
        <a>Sign up</a>
      </div>
      <h6 class="pt-2 text-xl"">Sign-up as a new user</h6>
      <form action=" /users" method="post">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="py-3">
          <label for="usertName">First Name:</label>
          <input type="text" name="firstName" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
          <span class="text-sm text-red-700">
            <% if(messages.firstName ) {%>
              <%= messages.firstName %>
                <% } %>
          </span>
        </div>
        <div class="py-3">
          <label for="usertName">Last Name:</label>
          <input type="text" name="lastName" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
        </div>
        <div class="py-3">
          <label for="email">Email:</label>
          <input type="email" name="email" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
          <span class="text-sm text-red-700">
            <% if(messages.email ) {%>
              <%= messages.email %>
                <% } %>
          </span>
        </div>
        <div class="py-3">
          <label for="password">Password:</label>
          <input type="password" name="password" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
          <span class="text-sm text-red-700">
            <% if(messages.password ) {%>
              <%= messages.password %>
                <% } %>
          </span>
        </div>
        <div class="py-3">
          <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Sign
            up</button>
        </div>
        </form>
    </div>
  </div>
</body>

</html>